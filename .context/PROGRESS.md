# Progress

Living log of what's done. Update this whenever a stage advances or a meaningful piece lands.

> **PIVOT v4 (2026-04-11):** Project pivoted to narrative galaxy with markdown mesh. Data model moved from JSON blob in SQLite to typed markdown files. Entity types changed from clusters/groups/entries/wraps to solar systems/planets/concepts/stories. Pipeline simplified from 4 stages to 3 (Ingest/Structure/Stories). Stories are long-form character-driven narratives. See REVAMP.md for full history.

## Current State

### Schema & Design (Flint)
Status: **complete**

- [x] v4 entity type definitions (Source, Solar System, Planet, Concept, Story)
- [x] JSON API types (GalaxyData, SolarSystem, Planet, Concept, Story, WikiLinkIndex)
- [x] Server parsing specification (3-step: index, parse, assemble)
- [x] Frontend wikilink resolution spec
- [x] Journey shard with complete pipeline workflows, templates, and skills
- [x] Onboarding document for teammates

### Frontend — Chat Landing
Status: **complete**
- [x] Chat landing page (logo, wordmark "STELLA TACO", tagline, input, drag-drop)
- [x] History system removed — replaced by `TacoDashboard` route
- [x] Loading screen status messages with animated ellipsis + rotating per-stage copy
- [x] Mobile system (bottom-pinned input, safe-area)
- [x] Galaxy renderer foundation (ambient cosmic void, nebulae, stars, shooting stars)
- [x] Black-hole launch sequence (submit -> rocket -> warp -> galaxy)
- [x] Background polish — drifting/breathing nebula blobs, dot grid overlay, hero glow, noise texture, edge vignette
- [x] File upload limit: 10 MB total

### Frontend — 3D Galaxy
Status: **linked to live backend** — Three.js scene, force-graph, camera system, and the `apps/server-gemini/` API contract are wired together. Remaining work is feature/data polish, not backend linkage.

- [x] Three.js scene manager (useThreeScene composable — bloom, OrbitControls, starfield, resize)
- [x] 3D force-directed graph — galaxy view (GalaxyView.vue, d3-force-3d)
- [x] Particle stream edges
- [x] Camera fly-in + GSAP transition with warp-speed effect
- [x] Solar system drill-down (SolarSystemView.vue) — textured planets (20 types: 9 real NASA-style + 11 procedural exotic), Saturn rings, particle-formation sun matching galaxy preset; planet/soul positions use seeded uniform spherical sampling (not Fibonacci — linear at low counts); sun click navigates back to galaxy
- [x] HTML overlay labels (3D->2D projection)
- [x] Warp-speed effect
- [x] Stats page (StatsView.vue)
- [x] Mobile support
- [x] Concept souls — 3D `THREE.Sprite` objects in scene (canvas ghost texture, sine-wave bob, raycasting, collision-avoidance placement, distance opacity, occlusion)
- [x] Planet visited dots — 5px dot right of label, grey hollow → `#5ba8ff` filled on open, persisted to localStorage
- [x] Solar system progress rings — SVG circular ring in galaxy labels, `#5ba8ff` arc, tracks visited planets / total, persisted
- [x] Exploration state persistence — `visitedPlanetIds` + `collectedConceptIds` in `useMeshStore`, backed by localStorage, HUD restored on mount
- [x] Story reader sidebar — `StoryReader.vue` left rail with intro/scenes/conclusion, scene navigation, wikilink-aware prose, "Visit planet" buttons, available in both GalaxyView and SolarSystemView
- [x] Wikilink rendering in markdown bodies — `[[(Type) Name]]` resolved via `wikiLinkIndex` with case-insensitive + whitespace-normalised fallback; unresolved wikilinks render as plain text (no strikethrough)
- [x] Cross-system planet traversal — story "Visit planet" button traverses solar→galaxy→solar via query-param state passing, story state preserved as a "Resume" affordance on the trigger
- [x] Deep-space ambience — CSS radial gradient backgrounds, viewport-scaled hero stars + noise-textured nebula billboards behind the starfield, tight bloom thresholds so halos don't bubble around particle systems
- [x] Link frontend data layer to the live `apps/server-gemini/` `/api/galaxy/*` contract

### Backend — Pipeline
Status: **active via `apps/server-gemini/`**

The live backend serving the frontend is now `apps/server-gemini/`. It owns the upload flow, staged generation pipeline, markdown mesh, media serving, and cached `GalaxyData` envelope returned from `/api/galaxy/:id`.

- [x] Frontend linked to `apps/server-gemini/` on port `8889`
- [x] API route — frontend consumes `/api/galaxy/create` + `/api/galaxy/:id`
- [x] Workspace parser — backend returns parsed `GalaxyData` from the mesh workspace
- [x] Media serving — image/file references resolve through `/api/galaxy/:id/media/:filename`
- [ ] Remove legacy `apps/server/` + `apps/proxy/` stack from the repo after docs/scripts cleanup

Legacy infrastructure still present in the repo but no longer on the active path:
- [x] `apps/server/` — superseded
- [x] `apps/proxy/` — superseded

### Shared Types
Status: **needs rewrite for v4**

- [ ] Rewrite `packages/shared/types/` for v4 types (SolarSystem, Planet, Concept, Story, GalaxyData)
- [ ] Remove v3 types (Cluster, Group, Entry, Wrap, etc.)

---

## Changelog

_Newest at top._

- 2026-04-12 — **UX polish batch + AI galaxy titles** (`main`) — (1) **Project renamed to Stella Taco**: all user-facing copy (page titles, aria-labels, wordmarks, taglines) updated across `ChatLanding.vue`, `TacoDashboard.vue`, `StatsView.vue`, `index.html`, `api.ts`. (2) **AI-generated galaxy titles**: `db/client.ts` gained `updateGalaxyTitle(id, title)`. `pipeline/run.ts` gained `generateGalaxyTitle(ctx)` — fires a single `MODEL_FLASH` call (20 token budget) after both the one-shot and staged pipeline complete; sends solar system titles + one-line descriptions; strips non-alphanum chars; non-fatal (dashboard falls back to user-supplied title on failure). Bug fixed: initial implementation used `prompt:` field (not a valid `GenerateOpts` field) instead of `parts: [{ text: ... }]`. (3) **Journey button removed** from `ChatLanding.vue` — `HistoryButton`, `HistoryOverlay`, `historyOpen` ref, `openHistory`/`closeHistory` functions, `recents` ref, `listRecentGalaxies`/`GalaxyEntry` imports, and related CSS rules all removed. `addRecentGalaxy` kept so the dashboard still sees new galaxies. (4) **"Every one gets wrapped" tagline removed** from chat landing hero. (5) **File size limit: 100 MB → 10 MB** — `TOTAL_SIZE_LIMIT_BYTES` in `apps/client/src/lib/fileTypes.ts`, display label in `ChatInput.vue`, and backend `MAX_TOTAL_BYTES` in `apps/server-gemini/src/routes/galaxy.ts`. (6) **ChatGalaxyPage loading skeleton**: `v-if="loading"` skeleton with shimmer animation (circular glyph placeholder, title/hint bars, input box, log rows) shown while the initial `fetchGalaxyEnvelope` + `fetchSubmissions` are in-flight; content rendered with `v-else` after load. (7) **Loading screen status messages**: animated rotating text above the rocket animation in `ChatLanding.vue` — per-stage `STAGE_MESSAGES` record, fade-cycle with 3s + up-to-2.4s random jitter, 50% more cooldown than initial implementation. Animated ellipsis using per-keyframe `animation-timing-function: step-end` and `width` clipping (fixes "backwards" dot direction). Status pill: frosted-glass, fixed bottom-28%, centered. (8) **Onboarding tooltip relocated**: moved from `SolarSystemView` to `GalaxyView` (positioned below nav, top-left). (9) **Dashboard wordmark sizing**: logo `80px→110px`, wordmark `0.58→0.78rem`, eyebrow `0.6→0.85rem`, mobile logo `60→80px`. (10) **Soul placement near planets**: souls now anchor 3–9 units from the surface of their connected planet (belt fallback if no planet found). Soul-to-soul repulsion added: `SOUL_MIN_SEP=9`, 20 push-apart iterations, tracks `placedSoulPositions[]`. (11) **Unique particle presets**: `getOrGenerateSystemPreset` in `meshStore.ts` now picks from unused presets first, only falls back to full pool when all presets are taken — minimises duplicate formation shapes. (12) **Desktop story+planet two-rail coexistence**: `openPlanetById`, `openConcept`, and `onStoryOpened` in `SolarSystemView` now only force-close the other rail on mobile; desktop keeps both open simultaneously. (13) **Story section click fixed**: `setScene()` in `StoryReader.vue` no longer emits `visit-planet` — only the explicit "Visit planet" button triggers planet navigation. (14) **Same-system story reopen after planet visit**: `onStoryVisitPlanet` in `SolarSystemView` captures story state, then calls `openByIdAtScene` 1400ms after the planet transition (accounts for 950ms fly + 350ms drawer). (15) **Bloom bubble eliminated**: `bloomRadius: 0.35→0` in `SolarSystemView` removes spherical halo spread. (16) **Galaxy compaction**: force-simulation spawn `±30/15→±14/7`, charge `-40→-18`, collide `10→7`.

- 2026-04-12 — **Append pipeline now generates new stories** (`main`) — `append.ts` was never calling `runStoryPitchesStage` / `runWriteStoriesStage`, so appended galaxies only ever had the original story. Fixed by adding `buildFullCtxForStories()` which re-reads the freshly-cached galaxy after all expand work, converts existing `MeshPlanet`/`MeshConcept` records back to `PlanetCtx`/`ConceptCtx` (using first sentence of markdown as `oneLineHook`), merges with any newly-created solar systems in ctx, and runs both story stages on the merged context. New stories are written to the workspace and cached; existing stories are untouched (append is additive).

- 2026-04-12 — **Append flow, nav redesign, mobile HUD, text contrast, bug fixes** (`main`) — (1) **Append loading screen**: `ChatGalaxyPage` no longer awaits the upload before navigating; instead it writes the pending payload to a module-level singleton (`pendingAppend.ts`) and immediately pushes to the new `/galaxy/:id/loading` route (`GalaxyLoadingPage.vue`), which fires the upload concurrently with the rocket animation — no freeze. (2) **First generation navigation**: `ChatLanding` now routes to `/galaxy/:id` (the 3D view) instead of `/galaxy/:id/chat` on completion. (3) **"Queuing…" flash fix**: `galaxyStatus` in `ChatGalaxyPage` now defaults to `'complete'` (not `'queued'`) when the envelope is null during initial load, so returning from the galaxy never flashes a spurious status pill. (4) **Solar system chat button fix**: `SolarSystemView`'s `onBeforeRouteLeave` guard was always hard-coding `router.push({ name: 'galaxy' })` — now correctly uses `router.push(to)` so the Chat button actually goes to the chat route. (5) **Nav redesign**: Home/Chat/Stories buttons replaced with a grouped card (shared border `radius:10px`, hairline dividers, no gaps) in both `GalaxyView` and `SolarSystemView`; `StoryReader` trigger restyled to match. (6) **Mobile hamburger**: on mobile, nav collapses to a hamburger icon (top-right) with a dropdown; desktop grouped card hidden via CSS. Applied in both `GalaxyView` and `SolarSystemView`. (7) **ConceptHUD in GalaxyView**: `ConceptHUD` + `ConceptDrawer` added to the galaxy scene (bottom-right, all screen sizes) — was previously only in solar system view. (8) **ChatGalaxyPage polish**: `max-width` 600→760px, glyph 160→200px, font sizes up, text opacity raised throughout for readability, background lightened `#02040a→#050810`. (9) **Append pipeline links bug fix**: `expandAdditionEntities()` in `append.ts` was passing `planetConnections: []` and `conceptConnections: []` to `writePlanet`/`writeConcept` — appended planets had 0 links. Fixed by adding `extractWikilinksFromBody()` which regexes `[[(Planet) Title]]` and `[[(Concept) Name]]` out of the canonicalized body and passes the extracted titles to the write functions.

- 2026-04-12 — **Bug fixes + multi-PDF solar system fix** (`main`) — Six frontend bugs fixed and committed (f92ed8a): (1) `setGalaxy is not defined` crash on launch removed from `ChatLanding.vue`; (2) planet and soul sprite placement in `SolarSystemView` switched from Fibonacci sphere (linear at low counts) to per-entity seeded uniform spherical sampling — proper 3D scatter at any count; (3) clicking the central sun in `SolarSystemView` now navigates back to galaxy via an invisible hit-sphere (`isSun` userData); (4) connected-planets chip row in `PlanetDrawer` now scrolls horizontally on scroll-wheel via `@wheel.prevent`; (5) `GalaxyView` no longer re-fetches and re-shows the loading screen on return from `SolarSystemView` — skips `loadFromApi` when `meshStore` already holds the correct galaxy; (6) broken wikilinks (AI-hallucinated entity names) no longer render with strikethrough — `renderMarkdownSectioned` adds case-insensitive + whitespace-normalised fallback lookup, and `EntityProse` renders any still-broken link as plain body text. Two backend fixes pending commit: one-shot pipeline restricted to single-source uploads (was incorrectly handling multi-PDF uploads by forcing everything into one solar system); cluster stage bypass lowered from `n ≤ 3` to `n = 1`; cluster prompt now explicitly hints to separate clearly-different topics for small source counts.

- 2026-04-12 — **Frontend/backend linkage finalized on `server-gemini`** — active client integration now targets `apps/server-gemini/` through the existing Vite `/api` proxy to port `8889`; `.context` docs updated to treat `server-gemini` as the live backend and the old `apps/server/` + `apps/proxy/` stack as legacy pending deletion.

- 2026-04-11 — **Space rift tuning, edge thickness, browser-back animation** (`feat/frontend-finalisation`) — (1) **Space rift visual fix**: initial rift was too bright and chunky (looked like a broad glowing beam); filament sigmas reduced `11 → 3px` (main spine), secondaries `6–7 → 2px`, so each filament is a tight crack not a wide band; sprite opacity reduced `0.80 → 0.22` (primary) and `0.40 → 0.10` (secondary halo); world-space scale reduced `520×130 → 320×80` so the rift subtends ~22°×5° of sky instead of ~68°×19°; added a very-wide low-brightness halo pass (sigma=22, brightness=0.12) behind the tight filaments for soft depth without blowing out. (2) **Galaxy particle-stream edge thickness**: inter-system dotted stream particle `size` bumped `0.55 → 0.85` for better visibility. (3) **Browser back button animation**: `SolarSystemView` now registers an `onBeforeRouteLeave` guard that intercepts the browser's native back button — cancels the nav, plays the standard veil-fade + warp-in animation, then re-navigates with a `bypassLeaveGuard` flag so the guard doesn't fire twice; programmatic exits (`navigateBack`, `travelToOtherSystemPlanet`) also set the flag before their `router.push` to prevent double animation.

- 2026-04-11 — **Mobile-first polish, textured-particle galaxy, persistent presets, space rift** (`feat/frontend-finalisation`) — Major batch of UX + visual fixes. **Mobile**: galaxy + system HUD text hidden via `@media (max-width: 768px)` so the top bar stays clear; Stories trigger and Galaxy back button collapse to icon-only pills on mobile; back button moved to bottom-left corner on both desktop and mobile (was top-left); `DrawerShell.mobile` is now true full-screen (`100dvh`, `border-radius: 0`); `EntityHero` mobile media query shrinks padding/title/stat sizes for more body room; galaxy view defaults to a more zoomed-out `cameraZ: 195` on mobile; galaxy view doesn't bind `mousemove` on mobile so taps don't flash a hover tooltip; `useThreeScene` viewport-scale floor `0.55 → 0.75` so phones still feel star-rich. **Souls HUD**: starts collapsed instead of auto-expanded; new click-outside-to-close watcher (window mousedown listener added on expand, removed on collapse, deferred via `setTimeout(0)` so the open click doesn't immediately close it). **Drawer transitions**: `onStoryVisitPlanet` in both views now waits for the 380ms drawer slide-out animation before flying the camera or kicking off cross-system traversal — no more visual overlap between closing/opening panels. **Galaxy view visual overhaul**: bloom completely off (`bloomStrength: 0`); switched to **soft textured particles** for both stars and system formations — `makeSoftPointTexture` (in `useThreeScene`) and `makeParticleTexture` (in `GalaxyView`) generate small canvas radial-gradient textures used as `map` on `PointsMaterial` so each particle is a self-glowing dot, no bloom needed; star size `0.5 → 1.6`, hero star size `1.4 → 3.0×vp`; system particle size `0.22 → 1.10` (mobile) / `0.85` (desktop); particle count `320 + planetCount*18 → 600 + planetCount*30`; atom preset's electron-head brightness capped (`glow * 0.55`) so additive accumulation doesn't blow out into a hard blob; bubble halos are now structurally impossible because there's no bloom pass amplifying anything. **Persistent random presets**: `meshStore` gained `systemPresets: Record<string, string>` persisted to `scholarSystem.presets:<galaxyId>` localStorage. New exposed `getOrGenerateSystemPreset(systemId, availablePresets)` picks via `Math.random()` on first generation, returns the persisted choice on subsequent calls; both views call it instead of seeded RNG so each galaxy is genuinely randomized at first generation but stable on refresh. Stale entries are pruned on hydrate. **Sun color fix**: `SolarSystemView` was picking from a 12-color `PLANET_COLORS` array while `GalaxyView` used an 8-color `SYSTEM_COLORS` — same seed, different array sizes → mismatched colors. Added a mirror `SYSTEM_COLORS` constant in solar view for the central sun pick. **Hover tooltip removed**: galaxy view's hover-name popup deleted from template + script + CSS; `mousemove` now only updates the cursor (`pointer` on hit, `grab` otherwise). **Space rift** (background detail added by user/linter): `useThreeScene` now has a `showRift` opt-in (default `true`) that renders an elongated filamentary "California Nebula"-style sprite in the upper-right of the sky — two overlapping `Sprite`s (sharp violet spine + dimmer indigo halo) using a 512×128 canvas texture with baked-in violet/indigo colors and a multi-filament FBM-noise mask, additive-blended, fog-disabled, slow co-rotation with the nebulas. Particle-stream edges between systems bumped (opacity `0.1 → 0.45`, size `0.4 → 0.55`).

- 2026-04-11 — **UX polish: souls rename, story trigger label, onboarding, drawer improvements, connection lines** (`feat/frontend-finalisation`) — (1) **Concept → Soul rename in UI**: `EntityHero` now shows "Soul" as the type tag when `type='concept'`; all user-facing copy updated. (2) **Story trigger expanded to pill**: `StoryReader.vue` trigger button is now a labeled pill "Stories" (icon + text) instead of a bare icon. (3) **"Threads through" chip row in ConceptDrawer**: up to 5 connected planet chips appear above the prose body, each clickable to navigate-to-planet. (4) **First-visit onboarding tooltips** (`OnboardingTooltip.vue`): 3 dismissable tooltips (localStorage-flagged per key) shown only for the user's very first galaxy (`ss.firstGalaxyId` global key set on first load); galaxy view tooltip "Each cluster is a theme from your upload. Click one to enter."; solar system view "Planets hold the knowledge. Souls float between them — click to collect." + "Characters journey through this galaxy — read their stories." near the Stories trigger. (5) **Soul HUD polish**: toggle button replaced gear icon with soul ghost SVG; moved toggle to bottom of flex column so the body panel grows upward without the button jumping; expand animation uses spring easing (`cubic-bezier(0.34, 1.2, 0.64, 1)`); max-height capped at exactly 3 grid rows (`calc(3 * 63px + 2 * 6px + 20px) = 221px`) then scrolls. (6) **Soul collect animation**: 3D sprite now hidden immediately on click (render loop gates on `inFlightSoulIds`) so the flying HTML element appears to BE the soul leaving the scene. (7) **Interactive scene with drawer open**: removed `rightStackOpen.value` guard from `onClick`/`onMouseMove` — planets and souls are now clickable in the 3D scene regardless of drawer state; direct map clicks use `replace` on the stack, wikilink navigation uses `push`. (8) **Planet labels visible when drawer open**: removed `v-if="!rightStackOpen"` wrapper; added `v-show="lbl.id !== currentRightPlanet?.id"` to hide only the active planet's label. (9) **Planet-to-planet connection lines**: pre-built as hidden `THREE.Line` objects (shader-driven animated dashes) for every connected pair; sun-to-planet spokes removed; on planet drawer open, that planet's outgoing lines fade in (GSAP opacity tween); dash animation via custom `ShaderMaterial` with `uTime` uniform driving `mod(vDist - uTime, period)` in fragment shader — dashes march away from the open planet toward connected planets; two directed lines per pair (forward + reverse) so direction is always correct; dash colour = destination planet's colour; density `uDash: 1.4, uGap: 1.2`; soft centre glow baked into shader.

- 2026-04-11 — **Story reader, cross-system traversal, deep-space ambience** (`feat/frontend-finalisation`) — `StoryReader.vue` now lives in both `GalaxyView` and `SolarSystemView` (shared `<DrawerShell>` left rail). Cross-system "Visit planet" works end-to-end via query-param state passing: solar→galaxy auto-enters target system→solar opens planet drawer; story state (active story id + scene index) is captured before transit and restored on arrival as a "Resume" affordance — colored left border + glow on the Stories trigger button (label always reads "Stories"). One-rail-at-a-time pattern: opening a planet/concept calls `storyReaderRef.collapse()` which preserves story state; opening the story emits `opened`, which clears `rightStack` via `onStoryOpened`. `useThreeScene` gained opt-in `heroStarCount` + `nebulaCount` layers with viewport-scaled density (`Math.sqrt(area / (1440·900))` clamped to `[0.55, 1.6]`); nebulas use Gaussian-falloff sprites with subtle FBM noise modulation, hue-coherent regions of 2–3 layered billboards in a desaturated slate-purple palette, all `fog: false` so they survive past the fog far plane. Renderer switched to `alpha: true` so a CSS `radial-gradient` (`#0a0618 → #02030a`) shows through both views as a deep-space tint; fog color shifted to `0x06081a` to match. Galaxy bloom retuned (`1.2/0.55/0.05 → 0.85/0.28/0.28`) — high threshold + small radius eliminates the spherical "bubble" halos that the gradient revealed. Galaxy particle-stream edges bumped (opacity `0.1 → 0.45`, size `0.4 → 0.55`). Solar bloom raised slightly (`0.04 → 0.18`, threshold `0.6`) so hero stars glow without lighting up planets. Solar sun particle count bumped 2.5× (`320 + planetCount*18 → 800 + planetCount*45`) with smaller point size for finer structure at close range. Galaxy back button shifted `left:72 → left:140` to clear the Stories trigger.

- 2026-04-11 — **Soul sprites, exploration tracking, progress rings** (`feat/frontend-finalisation`) — Concept souls converted from HTML overlays to `THREE.Sprite` 3D objects (canvas ghost texture, NormalBlending at low alpha for see-through effect, sine-wave bob, raycasting click/hover, collision-avoidance from planets, inner-to-mid belt placement 22–40 units). Bloom dialled way down (strength 0.04, threshold 0.35). Planet labels now show a visited dot (grey hollow → `#5ba8ff` filled). Galaxy view system labels now show an SVG circular progress ring (`#5ba8ff`, always-visible grey track). `useMeshStore` extended with `visitedPlanetIds` + `collectedConceptIds` refs backed by localStorage — both survive refresh. ConceptHUD rehydrated on mount from persisted IDs.

- 2026-04-11 — **SolarSystemView visual overhaul** (`feat/frontend-finalisation`) — replaced monotone glowing spheres with textured planets. 20 planet types seeded from planet ID (type drives both size and surface): 9 real Solar System Scope 2K textures (earth, mars, moon, jupiter, saturn, uranus, neptune, venus, mercury) + 11 exotic procedural canvas textures (lava, ice, desert, toxic, crystal, dead_rock, ocean, gas_purple, ice_giant, molten, void). Saturn type gets `RingGeometry` with radial-UV remapping. Central sun swapped from solid `MeshStandardMaterial` sphere to the same particle formation the galaxy view uses for that system (same `seededRng(sys.id + '_preset')` → shape continuity), scaled to 60%. Textures preload via `THREE.TextureLoader` before `buildScene()` fires; veil stays black as the load screen. Download script at `apps/client/scripts/download-planet-textures.sh` fetches the Solar System Scope pack into `public/textures/planets/`. Lighting fix: Three.js 0.183's physical lights made `PointLight` fall off by inverse-square so the sun light wasn't reaching planets at all — switched to `DirectionalLight` for the key light and added `emissiveMap: tex` so each planet self-lights through its texture regardless of point-light positioning. Thin white `opacity 0.08` connection lines from sun to each planet. `makeExoticTexture` seeds from `planetId + typeId` so two planets of the same exotic type don't look identical.
- 2026-04-11 — **PIVOT v4: Narrative Galaxy — Markdown Mesh** — data model moved from JSON blob to markdown mesh. Entity types: Source, Solar System, Planet, Concept, Story. Pipeline: 3 stages (Ingest/Structure/Stories). Stories are character-driven narratives. Journey shard built with workflows, templates, skills. Schema spec complete.
- 2026-04-11 — (superseded) PIVOT v3: Wrap-based Memory Galaxy — every node is a Spotify-Wrapped card. Pipeline 4 stages. Schema 7 scopes. Replaced by v4 same day.
- 2026-04-11 — (superseded) Phase 4: v3 pipeline cleanup — 18 old files deleted
- 2026-04-11 — (superseded) Phase 3: v3 runner + API
- 2026-04-11 — (superseded) Phase 2: v3 pipeline rebuild
- 2026-04-11 — (superseded) Phase 1: v3 Schema
- 2026-04-11 — Frontend polish pass — galaxy/solar system labels, bloom tuning, warp-speed effect, exploration UI, mobile fixes, landing page background effects
- 2026-04-11 — 3D frontend complete — GalaxyView, SolarSystemView, WrapCard, StatsView, useThreeScene, useWarpEffect, mockGalaxy fixture
- 2026-04-10 — Stages 2, 2.5, 3 implemented (v2 pipeline, superseded)
- 2026-04-10 — Proxy skeleton landed
- 2026-04-10 — Chat landing, history, mobile, black-hole launch
- 2026-04-09 — Project scaffolded
