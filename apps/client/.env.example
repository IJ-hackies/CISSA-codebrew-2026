# Backend API base URL. Leave empty for local dev (Vite proxy handles it).
# Set to the full origin for split deploys (e.g. frontend on Vercel, backend on DO).
# VITE_API_URL=http://134.199.156.237:8889
